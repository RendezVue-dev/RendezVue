import React from 'react';

const GroupCard = ({ group }) => {
  return (
    <div className="group-card">
      {/* TODO: Display group info, members, events, etc. */}
      <h2>{group.name}</h2>
      {/* ... */}
    </div>
  );
};

export default GroupCard;
